#include <bits/stdc++.h>
using namespace std;
//11066¹ø ¹®Á¦
void FileMerge() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	int T;
	cin >> T;
	while (T > 0) {
		int K;
		cin >> K;
		vector<int> vc(K);
		for (int i = 0; i < K; i++) 
			cin >> vc[i];
		
		T--;
	}
}